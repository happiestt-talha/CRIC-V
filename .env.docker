# Shared environment variables for Docker
# Copy this to .env.docker and fill in values
# NEVER commit real secrets to Git

# PostgreSQL
POSTGRES_DB=cricv_db
POSTGRES_USER=cricv_user
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD_123
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Redis
REDIS_URL=redis://redis:6379/0

# Backend
SECRET_KEY=CHANGE_THIS_TO_RANDOM_64_CHAR_STRING
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Frontend
NEXT_PUBLIC_API_URL=http://localhost/api
NEXT_PUBLIC_SITE_URL=http://localhost

# Email (update with real values)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=mtalhamanzoor1930@gmail.com
MAIL_PASSWORD=rpqleukcruuyemry
MAIL_FROM=mtalhamanzoor1930@gmail.com
MAIL_FROM_NAME=CRIC-V

# Environment
ENVIRONMENT=development
